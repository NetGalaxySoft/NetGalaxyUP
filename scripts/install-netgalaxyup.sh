#!/bin/bash

# ==========================================================================
#  install-netgalaxyup.sh – Автоматична инсталация на проекта NetGalaxyUP
# --------------------------------------------------------------------------
#  Версия: 1.2
#  Дата: 2025-07-02
#  Автор: Ilko Yordanov / NetGalaxy
# ==========================================================================
#
#  Скриптът клонира проекта NetGalaxyUP от GitHub, инсталира нужните
#  зависимости и подготвя средата. Работи без интеракция.
# ==========================================================================

echo "=================================================================="
echo " 🚀 NetGalaxyUP – Автоматична инсталация"
echo "=================================================================="

# Очакваме GitHub URL с вграден токен като аргумент
REPO_URL="$1"

if [[ -z "$REPO_URL" ]]; then
  echo "❌ Липсва GitHub URL с токен."
  echo "   Използвай следния синтаксис:"
  echo
  echo "   curl -fsSL https://raw.githubusercontent.com/NetGalaxySoft/NetGalaxyUP/main/scripts/install-netgalaxyup.sh | bash -s https://NetGalaxySoft:<TOKEN>@github.com/NetGalaxySoft/NetGalaxyUP.git"
  echo
  exit 1
fi

# Създаване на директория ~/projects ако не съществува
mkdir -p ~/projects
cd ~/projects || exit 1

# Проверка дали проектът вече съществува
if [[ -d "NetGalaxyUP" ]]; then
  echo "✅ Проектът NetGalaxyUP вече съществува. Пропускане на клонирането."
else
  echo "⏳ Клониране на проекта от GitHub..."
  git clone "$REPO_URL"
fi

cd NetGalaxyUP || exit 1

# --------------------------------------------------------------------------
# Инсталиране на Python зависимости (backend)
# --------------------------------------------------------------------------
echo "=================================================================="
echo " 🐍 Инсталиране на Python зависимости (backend)..."
echo "=================================================================="

cd backend || exit 1
python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
deactivate
cd ..

# --------------------------------------------------------------------------
# Инсталиране на Node.js зависимости (frontend)
# --------------------------------------------------------------------------
echo "=================================================================="
echo " 🌐 Инсталиране на Node.js зависимости (frontend)..."
echo "=================================================================="

cd frontend || exit 1
npm install
cd ..

# --------------------------------------------------------------------------
# Финално съобщение
# --------------------------------------------------------------------------
echo "=================================================================="
echo " ✅ Проектът NetGalaxyUP е успешно инсталиран."
echo "=================================================================="
